.nav {
    display: flex;
    align-items: center;
    margin-right: 20px;
    &__list, &__item {
        display: flex;
    }
    &__item {
        margin-right: 20px;
        cursor: pointer;
    }
    &__item:last-child {
        margin: 0;
    }
}
